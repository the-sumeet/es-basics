Standalone script can submit bulk data into elasticsearch via REST-API

`Logstash` and `beats` can stream data from logs, s3, database, and more

Kafka, spark, and more have elasticsearch integration add-ons.
